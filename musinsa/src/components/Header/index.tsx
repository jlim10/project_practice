import * as T from './Styled';

export const Header = () => {
    return (
      <T.HeaderContainer>
        <T.HeaderContent>
            Header
        </T.HeaderContent>
      </T.HeaderContainer>  
    );
}